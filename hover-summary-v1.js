/* Lost Ark Party — compact hover summary authority v5 */
(()=>{
'use strict';
const clean=s=>String(s??'').replace(/\s+/g,' ').trim();
const num=s=>{const m=String(s??'').replace(/,/g,'').match(/[-+]?\d+(?:\.\d+)?/);return m?Number(m[0]):0};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const SUPPORTS=new Set(['Bard','Artist','Paladin','Valkyrie']);
const members=()=>[...document.querySelectorAll('#suggestedParties .party-member')];
const card=m=>m?.querySelector('.character-hover-breakdown');
function role(m){const r=clean(m?.querySelector('.party-role-label')?.textContent).toLowerCase();if(r==='support'||r==='dps')return r;const c=clean(m?.querySelector('.class-icon')?.alt||m?.dataset.class||'');return SUPPORTS.has(c)?'support':'dps'}
const name=m=>clean(card(m)?.querySelector('.chb-head strong')?.textContent||m?.querySelector('.party-character-link')?.textContent);
function cp(m){return num(card(m)?.querySelector('.chb-head span')?.textContent.match(/CP\s+([\d,.]+)/i)?.[1])}
function raidMode(){return window.LostArkOptimizerMode?.general===false&&!!window.LostArkOptimizerMode?.raid}
function encounterLabel(){const e=window.LostArkOptimizerMode?.encounter||window.LostArkEncounterModel?.getProfile?.()||null;return clean(e?.label||e?.name||'').replace(/\s*[—–]\s*/g,' - ')}
function parseRows(m){const c=card(m);if(!c)return[];const sourceKey='compactSourceRowsV5';try{const saved=c.dataset[sourceKey];if(saved){const parsed=JSON.parse(saved);if(Array.isArray(parsed)&&parsed.length)return parsed}}catch{}const out=[];for(const row of c.querySelectorAll('.chb-synergy')){const t=clean(row.textContent);const x=t.match(/^(.+?)\s+from\s+(.+?):\s*([+-]?[\d,]+(?:\.\d+)?)\s+estimated contribution(?:\s*(?:·|-)?\s*([\d.]+)%\s+of\s+base\s+power)?(?:\s*(?:·|-)?\s*observed median uptime\s*([\d.]+)%?)?/i);if(!x)continue;out.push({effect:clean(x[1]),source:clean(x[2]),value:num(x[3]),pct:Number.isFinite(Number(x[4]))?Number(x[4]):0,uptime:Number.isFinite(Number(x[5]))?Number(x[5]):null})}if(out.length)c.dataset[sourceKey]=JSON.stringify(out);return out}
function group(rows,key){const map=new Map();for(const r of rows){const k=r[key];if(!map.has(k))map.set(k,{name:k,value:0,pct:0,uptime:null});const g=map.get(k);g.value+=r.value;g.pct+=r.pct;if(r.uptime!==null&&r.uptime!==undefined)g.uptime=r.uptime}return[...map.values()].sort((a,b)=>b.value-a.value)}
function replaceDetails(c,html){c.querySelectorAll('.chb-detail').forEach(d=>d.remove());const d=document.createElement('div');d.className='chb-detail chb-compact-detail';d.innerHTML=html;c.appendChild(d)}
function renderDps(m){if(role(m)!=='dps')return;const c=card(m);if(!c)return;const rows=parseRows(m);if(!rows.length)return;const groups=group(rows,'source');const supportNames=new Set(members().filter(x=>role(x)==='support').map(name));const synergyPct=rows.filter(r=>!supportNames.has(r.source)).reduce((a,r)=>a+r.pct,0);const supportPct=rows.filter(r=>supportNames.has(r.source)).reduce((a,r)=>a+r.pct,0);const stats=c.querySelector('.chb-stats');if(stats)stats.innerHTML=`<span>Party Synergy +${synergyPct.toFixed(2)}%</span><span>Support Impact +${supportPct.toFixed(2)}%</span><span>Support compatibility uses encounter data</span>`;const html=(raidMode()?`<div class="chb-compact-encounter">${esc(encounterLabel())}</div>`:'')+groups.map(g=>{const support=supportNames.has(g.name);return`<div class="chb-summary-row">+${g.value.toLocaleString(undefined,{maximumFractionDigits:2})} estimated ${support?'support':'synergy'} contribution from ${esc(g.name)} - ${g.pct.toFixed(2)}% of base power${support&&g.uptime!==null?` - observed median uptime ${g.uptime.toFixed(2)}%`:''}</div>`}).join('');replaceDetails(c,html)}
function renderSupport(m){if(role(m)!=='support')return;const c=card(m);if(!c)return;const party=m.closest('.party');const partyMembers=party?[...party.querySelectorAll('.party-member')]:members();const rows=[];for(const dps of partyMembers){if(dps===m||role(dps)!=='dps')continue;for(const r of parseRows(dps))if(r.source===name(m))rows.push({target:name(dps),value:r.value,pct:r.pct,uptime:r.uptime})}if(!rows.length)return;const groups=group(rows,'target'),total=groups.reduce((a,g)=>a+g.value,0),supportCp=cp(m),impact=supportCp?total/supportCp*100:0;const h=c.querySelector('.chb-head span');if(h)h.textContent=`CP ${supportCp.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})} - Contribution ${Math.round(supportCp+total).toLocaleString()}`;const stats=c.querySelector('.chb-stats');if(stats)stats.innerHTML=`<span>Party Synergy +0.00%</span><span>Support Impact +${impact.toFixed(2)}%</span><span>Support compatibility uses encounter data</span>`;const html=(raidMode()?`<div class="chb-compact-encounter">${esc(encounterLabel())}</div>`:'')+groups.map(g=>`<div class="chb-summary-row">+${g.value.toLocaleString(undefined,{maximumFractionDigits:2})} estimated support contribution to ${esc(g.name)} - ${g.pct.toFixed(2)}% of ${esc(g.name)}'s base power</div>`).join('');replaceDetails(c,html)}
function apply(){const ms=members();for(const m of ms){if(card(m))role(m)==='support'?renderSupport(m):renderDps(m)}}
function css(){let s=document.getElementById('compact-hover-style');if(!s){s=document.createElement('style');s.id='compact-hover-style';document.head.appendChild(s)}s.textContent='.chb-compact-detail{display:block!important;line-height:1.45;margin-top:6px}.chb-summary-row{display:block!important;margin:6px 0!important}.chb-compact-encounter{font-weight:600;margin:0 0 6px}.chb-compact-detail .chb-summary-row{opacity:1!important}'}
function start(){window.LostArkHoverSummaryV1={active:true,version:5};css();apply();const root=document.getElementById('suggestedParties')||document.body;let timer=0;const schedule=()=>{clearTimeout(timer);timer=setTimeout(apply,0)};new MutationObserver(schedule).observe(root,{childList:true,subtree:true,characterData:true});document.getElementById('optimizeBtn')?.addEventListener('click',()=>[0,20,50,100,200,400,800,1500,3000].forEach(ms=>setTimeout(apply,ms)),true);document.getElementById('raidSpecificSelect')?.addEventListener('change',()=>setTimeout(apply,0),true);[50,150,300,600,1000,2000,4000].forEach(ms=>setTimeout(apply,ms))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
