/* Final New Additions authority guard. Keeps class/spec resolution data-driven. */
(()=>{
  'use strict';
  const NEW_KEY='lostark-hideout-new-additions-v1';
  const CONNECTOR='https://lostark-bible-connector.seraph0226.workers.dev/character';
  const read=()=>{try{return JSON.parse(localStorage.getItem(NEW_KEY)||'[]')}catch{return[]}};
  const write=v=>{try{localStorage.setItem(NEW_KEY,JSON.stringify(v))}catch{}};
  const cls=c=>String(c?.profile?.class||c?.profile?.className||'').trim();
  const norm=v=>String(v??'').toLowerCase().replace(/[’']/g,"'").replace(/\s+/g,' ');
  const usableIcon=s=>{const v=String(s||'').trim();if(!/^https?:\/\//i.test(v)||!/\.(png|jpe?g|webp|svg)(\?|#|$)/i.test(v))return '';if(/poyoanon\.fyi|lostark\.fandom\.com/i.test(v))return '';return v};
  const profileText=c=>{try{return norm(JSON.stringify(c?.profile||{}))}catch{return ''}};
  const payloadText=data=>{try{return norm(JSON.stringify(data||{}))}catch{return ''}};
  function findValkIcon(value,key=''){const keyText=norm(key);if(value==null)return '';if(typeof value==='string'){if(/valkyrie|class.?icon|icon|avatar|image/.test(keyText))return usableIcon(value);return ''}if(Array.isArray(value)){for(const x of value){const hit=findValkIcon(x,key);if(hit)return hit}return ''}if(typeof value==='object'){const local=norm(JSON.stringify(value));const likely=/valkyrie/.test(local)&&/(icon|image|avatar|class)/.test(local);for(const [k,v] of Object.entries(value)){const hit=findValkIcon(v,likely?`${key} ${k} valkyrie`:k);if(hit)return hit}}return ''}
  function findValkIconInHtml(html){try{const d=new DOMParser().parseFromString(String(html||''),'text/html');for(const el of d.querySelectorAll('*')){const meta=[el.getAttribute?.('alt'),el.getAttribute?.('title'),el.getAttribute?.('aria-label'),el.getAttribute?.('class'),el.getAttribute?.('data-class'),el.getAttribute?.('data-character-class')].filter(Boolean).join(' ');if(!/valkyrie/i.test(meta))continue;const src=usableIcon(el.getAttribute?.('src')||el.getAttribute?.('data-src')||el.getAttribute?.('href'));if(src)return src;const style=el.getAttribute?.('style')||'';const m=style.match(/url\((['"]?)(https?:[^'"\)]+)\1\)/i);const styled=usableIcon(m?.[2]);if(styled)return styled}}catch{}return ''}
  async function payload(url){try{const r=await fetch(`${CONNECTOR}?url=${encodeURIComponent(url)}`,{cache:'no-store',headers:{Accept:'application/json'}});if(!r.ok)return null;return await r.json()}catch{return null}}
  async function repair(){const list=read();if(!Array.isArray(list)||!list.length)return;let changed=false;for(const c of list){if(!c?.profile)continue;const p=c.profile;let data=null;if(cls(c)==='Arcanist'||cls(c)==='Valkyrie')data=await payload(c.url);const text=`${profileText(c)} ${payloadText(data)}`;if(cls(c)==='Arcanist'){if(/order\s+of\s+the\s+emperor/.test(text)){if(p.spec!=='Order of the Emperor'){p.spec='Order of the Emperor';changed=true}}else if(/grace\s+of\s+the\s+empress/.test(text)){if(p.spec!=='Grace of the Empress'){p.spec='Grace of the Empress';changed=true}}}if(cls(c)==='Valkyrie'){const icon=findValkIcon(data)||findValkIconInHtml(data?.html||data?.characterHtml||data?.content||data?.page||'')||usableIcon(p.classIcon);if(icon&&p.classIcon!==icon){p.classIcon=icon;changed=true}const card=document.querySelector(`.new-addition-card[data-candidate-id="${CSS.escape(String(c.id))}"]`);const img=card?.querySelector('img.class-icon');if(img&&icon&&img.src!==icon){img.src=icon;img.alt='Valkyrie'}}}if(changed)write(list);for(const c of list){const card=document.querySelector(`.new-addition-card[data-candidate-id="${CSS.escape(String(c.id))}"]`);if(!card)continue;const spec=card.querySelector('.class');if(spec&&c.profile?.spec)spec.textContent=c.profile.spec;if(cls(c)==='Valkyrie'&&c.profile?.classIcon){const img=card.querySelector('img.class-icon');if(img){img.src=c.profile.classIcon;img.alt='Valkyrie'}}}}
  const run=()=>setTimeout(()=>{repair().catch(()=>{})},50);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run();
})();
