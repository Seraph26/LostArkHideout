/* Retired: General Optimization metrics are now rendered by general-party-optimizer-v1.js. */
(()=>{
  'use strict';
  // Intentionally inert. Keeping the file preserves the existing loader contract,
  // but prevents a second metrics/arrow/baseline system from competing with the
  // unified General optimizer.
})();
