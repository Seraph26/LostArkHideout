/* Lost Ark Hideout — General Optimization render guard + canonical DPS hover v21 */
(()=>{
'use strict';
const root=()=>document.querySelector('#suggestedParties');
const active=()=>document.querySelector('#generalOptimization')?.checked!==false&&window.LostArkOptimizerMode?.general!==false;
const clean=s=>String(s??'').replace(/\s+/g,' ').trim(),num=v=>{const n=Number(String(v??'').replace(/,/g,''));return Number.isFinite(n)?n:0},fmt=v=>Math.round(num(v)).toLocaleString(),pct=v=>`${v>=0?'+':''}${v.toFixed(2)}%`,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const LABELS={damage:'Damage',mana:'Mana',crit:'Critical Rate',critDamage:'Critical Damage',attackSpeed:'Attack Speed',attackPower:'Attack Power',supportAmplification:'Support Amplification',positional:'Positional Damage',moveSpeed:'Move Speed',movementSpeed:'Move Speed'};
const SUPPORTS=new Set(['Bard','Artist','Paladin','Valkyrie']);
function store(){try{return JSON.parse(localStorage.getItem('lostark-hideout-private-v3')||'null')||{characters:[]}}catch{return{characters:[]}}}
function roster(){return(store().characters||[]).filter(c=>c&&c.id)}
function profile(c){return c.profile||c.data||{}}
function build(c){try{return window.LostArkBuildProfilesV3?.get(c.url)||window.LostArkBuildProfilesV2?.get(c.url)||{}}catch{return{}}}
function text(c){const p=profile(c),b=build(c);return clean([p.rawText,p.text,p.characterText,p.arkGridText,p.arkPassiveText,p.engravingsText,p.gemsText,p.tripodsText,b.text,b.className,b.engravings?.join(' '),b.grid?.map(x=>x.name+' '+x.points).join(' '),b.arkPassive?.map(x=>x.name+' '+x.level).join(' ')].filter(Boolean).join(' ')).toLowerCase()}
function cls(c){const p=profile(c),b=build(c),v=clean(p.class||p.className||p.characterClass||b.className);if(v&&v.toLowerCase()!=='unknown')return({'Soul Eater':'Souleater','Arcana':'Arcanist','Glavier':'Glaivier'}[v]||v);const t=text(c),m=[['Summoner',/\bsummoner\b|master summoner|ancient spear/],['Souleater',/souleater|soul eater|full moon harvester|night.?s edge/],['Arcanist',/arcanist|arcana|empress grace|emperor.?s decree/],['Glaivier',/glaivier|glavier|pinnacle|control/],['Bard',/\bbard\b|desperate salvation|true courage/],['Artist',/\bartist\b|full bloom|recurrence/],['Paladin',/\bpaladin\b|blessed aura/],['Valkyrie',/\bvalkyrie\b/]];for(const[n,r]of m)if(r.test(t))return n;return'Unknown'}
function role(c){const p=profile(c);return p.role==='Support'||SUPPORTS.has(cls(c))?'Support':'DPS'}
function positional(c){const b=build(c),t=text(c);if(b.positional&&b.positional!=='Unknown')return b.positional;if(/ambush master|back attack/.test(t))return'Back Attack';if(/master brawler|front attack/.test(t))return'Front Attack';if(/hit master/.test(t))return'Hit Master';return'Unknown'}
function effects(c){try{const x=window.LostArkPartySynergyAuthorityV1?.provided?.(cls(c),build(c),text(c));if(Array.isArray(x)&&x.length)return x}catch{}return[]}
function weight(c,type){const t=text(c),p=positional(c);let w=1;if(type==='crit'&&/keen blunt|adrenaline|burst|full moon/.test(t))w+=.35;if(type==='critDamage'&&/keen blunt|burst/.test(t))w+=.30;if(type==='attackSpeed'&&/raid captain|swiftness/.test(t))w+=.35;if(type==='positional')w=p==='Back Attack'||p==='Front Attack'?1.35:p==='Hit Master'?.55:.85;if(type==='mana')w=/summoner|mana|boundless/.test(t)?1.55:.2;if(type==='damage'&&/summoner/.test(t))w+=.10;return w}
function supportEffects(c){switch(cls(c)){case'Bard':return[{type:'supportAmplification',value:.10},{type:'mana',value:.12},{type:'attackSpeed',value:.035}];case'Artist':return[{type:'supportAmplification',value:.10},{type:'mana',value:.08},{type:'attackSpeed',value:.04}];case'Paladin':return[{type:'supportAmplification',value:.10},{type:'damage',value:.03}];case'Valkyrie':return[{type:'supportAmplification',value:.095},{type:'attackSpeed',value:.06}];default:return[{type:'supportAmplification',value:.09}]}}
function parties(){const state=(()=>{try{return JSON.parse(localStorage.getItem('lostark-hideout-party-assignments-v2')||'null')}catch{return null}})()||{};const m=new Map(roster().map(c=>[c.id,c])),out=[];for(const ids of [state.party1||[],state.party2||[]])out.push(ids.filter(id=>m.has(id)).slice(0,4).map(id=>m.get(id)));return out}
function model(p,target){if(role(target)!=='DPS')return null;const cp=num(profile(target).cp??profile(target).combatPower),sources=[];let synergy=0,support=0;for(const src of p.filter(c=>c!==target&&role(c)==='DPS'))for(const e of effects(src)){const v=cp*num(e.magnitude??e.value)*weight(target,e.type);if(v>0){synergy+=v;sources.push({source:profile(src).name||src.name,type:e.type,value:v,group:'synergy'})}}const sup=p.find(c=>role(c)==='Support');if(sup)for(const e of supportEffects(sup)){const v=cp*e.value*weight(target,e.type);if(v>0){support+=v;sources.push({source:profile(sup).name||sup.name,type:e.type,value:v,group:'support'})}}return{cp,synergy,support,sources}}
function existingTotals(card,cp){const raw=clean(card.textContent);const find=label=>{const m=raw.match(new RegExp(label+'\\s*:?\\s*([+-]?[\\d,]+(?:\\.\\d+)?)','i'));return m?num(m[1]):0};return{synergy:card.dataset.generalSynergyValue!=null?num(card.dataset.generalSynergyValue):find('Party Synergy'),support:card.dataset.generalSupportValue!=null?num(card.dataset.generalSupportValue):find('Support Impact'),total:find('Contribution')||cp}}
function canonical(member,party){if(!active()||!member||role(member)!=='DPS')return;const card=member.querySelector('.character-hover-breakdown');if(!card)return;const cp=num(profile(member).cp??profile(member).combatPower);if(!cp)return;const m=model(party,member);if(!m)return;const old=existingTotals(card,cp),synergy=old.synergy||m.synergy,support=old.support||m.support,total=old.total||cp;card.dataset.generalSynergyValue=String(synergy);card.dataset.generalSupportValue=String(support);const lines=[];for(const group of ['synergy','support']){const rows=m.sources.filter(x=>x.group===group),sum=rows.reduce((a,x)=>a+x.value,0),target=group==='synergy'?synergy:support;if(sum>0)for(const r of rows){const v=r.value/sum*target;lines.push(`<div class="chb-synergy">${esc(LABELS[r.type]||r.type)} from ${esc(r.source)}: ${v>=0?'+':''}${fmt(v)} estimated contribution<div class="chb-contribution-pct">${(v/cp*100).toFixed(2)}% of base power</div></div>`)}}card.className='character-hover-breakdown chb-canonical-dps';card.innerHTML=`<div class="chb-head"><strong>${esc(profile(member).name||member.name)}</strong><span>CP ${cp.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})} - Contribution ${fmt(total)}</span></div><div class="chb-stats"><span>Party Synergy ${pct(synergy/cp*100)}</span><span>Support Impact ${pct(support/cp*100)}</span></div><div class="chb-detail">Support compatibility uses general party data</div><div class="chb-detail">${lines.join('')||'<div>No direct party effects detected.</div>'}</div>`;card.dataset.generalCanonical='1'}
function render(){if(!active())return;for(const p of parties())if(p.length===4)for(const c of p)canonical(c,p)}
function observe(){const h=root();if(!h)return;new MutationObserver(()=>{if(active())render()}).observe(h,{childList:true,subtree:true,characterData:true});[0,100,250,500,1000,2000,4000].forEach(ms=>setTimeout(render,ms));setInterval(render,1000)}
function css(){let s=document.getElementById('general-canonical-dps-style');if(!s){s=document.createElement('style');s.id='general-canonical-dps-style';document.head.appendChild(s)}s.textContent='.chb-canonical-dps{width:430px!important;text-align:left}.chb-canonical-dps .chb-head{display:flex;justify-content:space-between;gap:10px;margin-bottom:5px}.chb-canonical-dps .chb-stats{display:flex;gap:16px;margin:3px 0 5px}.chb-canonical-dps .chb-detail{margin-top:5px}.chb-canonical-dps .chb-synergy{display:block;margin:4px 0}.chb-canonical-dps .chb-contribution-pct{margin-top:2px}'}
function start(){css();observe();render()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
