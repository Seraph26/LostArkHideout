/* Lost Ark Hideout — General Optimization top-potential swap indicators */
(()=>{
'use strict';
let baseline=null,captureTimer=null,armed=false,renderTimers=[];
const root=()=>document.getElementById('suggestedParties');
const general=()=>!!document.getElementById('generalOptimization')?.checked;
const clean=s=>String(s??'').replace(/\s+/g,' ').trim();
const num=s=>{const n=Number(String(s??'').replace(/[^0-9.+-]/g,''));return Number.isFinite(n)?n:null};
const names=()=>{try{const x=JSON.parse(localStorage.getItem('lostark-hideout-private-v3')||localStorage.getItem('lostark-hideout-private-v2')||'null');return new Map((x?.characters||[]).map(c=>[String(c.id),clean(c.profile?.name||c.name||c.id)]))}catch{return new Map()}};
function state(){const r=root();if(!r)return{p1:[],p2:[]};const z=[...r.querySelectorAll('.authoritative-dropzone')].slice(0,2);return{p1:[...z[0]?.querySelectorAll('.authoritative-member[data-character-id]')||[]].map(x=>x.dataset.characterId),p2:[...z[1]?.querySelectorAll('.authoritative-member[data-character-id]')||[]].map(x=>x.dataset.characterId)}}
function swapText(a,b,eventDetail){if(eventDetail?.sourceId&&eventDetail?.targetId){const n=names();return `${n.get(String(eventDetail.sourceId))||eventDetail.sourceId} swapped with ${n.get(String(eventDetail.targetId))||eventDetail.targetId}`}const n=names(),out=[];for(const p of ['p1','p2']){const x=new Set(a[p]||[]),y=new Set(b[p]||[]);for(const id of x)if(!y.has(id))out.push(id)}return out.length===2?`${n.get(String(out[0]))||out[0]} swapped with ${n.get(String(out[1]))||out[1]}`:'Manual party swap'}
function parties(){const r=root();if(!r)return[];return [...r.querySelectorAll('.authoritative-party .party-score')].map(e=>{const strong=e.querySelector('strong');const m=clean(e.textContent).match(/Estimated potential:\s*([\d,.]+)/i);const v=num(strong?.textContent||m?.[1]);return v==null?null:{e,v}}).filter(Boolean).slice(0,2)}
function combined(){const r=root();if(!r)return null;const e=r.querySelector('.authoritative-party .party-footer');if(!e)return null;const strong=e.querySelector('strong');const m=clean(e.textContent).match(/Combined estimated potential:\s*([\d,.]+)/i);const v=num(strong?.textContent||m?.[1]);return v==null?null:{e,v}}
function arrow(delta,base,title){if(Math.abs(delta)<.005)return'';const up=delta>0,value=Math.abs(Math.round(delta)).toLocaleString(),pct=base?Math.abs(delta/base*100).toFixed(2):'0.00';return `<span class="general-top-swap-arrow ${up?'general-swap-up':'general-swap-down'}" data-swap-title="${clean(title)}">${up?'▲':'▼'} ${value} (${up?'+':'-'}${pct}%)</span>`}
function clearArrows(){root()?.querySelectorAll('.general-top-swap-arrow').forEach(e=>e.remove())}
function placeArrow(e,html){if(!e||!html)return;const old=e.querySelector('.general-top-swap-arrow');if(old)old.remove();const strong=e.querySelector('strong');if(strong)strong.insertAdjacentHTML('afterend',html);else e.insertAdjacentHTML('beforeend',html)}
function render(title){if(!general()||!baseline||!armed)return;const p=parties(),c=combined();if(p.length<2||!c)return;p.forEach((x,i)=>{const b=baseline.p?.[i];if(b!=null)placeArrow(x.e,arrow(x.v-b,b,title))});if(baseline.c!=null)placeArrow(c.e,arrow(c.v-baseline.c,baseline.c,title))}
function schedule(title){for(const t of renderTimers)clearTimeout(t);renderTimers=[];[0,50,150,300,600,1000,1600].forEach(ms=>renderTimers.push(setTimeout(()=>render(title),ms)))}
function capture(){if(!general())return;clearArrows();const p=parties(),c=combined();if(p.length<2||!c){captureTimer=setTimeout(capture,500);return}baseline={sig:JSON.stringify(state()),state:state(),p:p.map(x=>x.v),c:c.v};armed=false}
function beginOptimize(){if(!general())return;armed=false;baseline=null;clearArrows();if(captureTimer)clearTimeout(captureTimer);captureTimer=setTimeout(capture,1500)}
function onSwap(e){if(!general()||!baseline)return;armed=true;const title=swapText(baseline.state,state(),e?.detail);clearArrows();schedule(title)}
function start(){const r=root();if(!r)return;document.addEventListener('click',e=>{if(e.target.closest?.('#optimizeBtn'))beginOptimize()},true);document.addEventListener('lostark:party-swap-applied',onSwap);const mode=document.getElementById('generalOptimization');mode?.addEventListener('change',()=>{if(!mode.checked){baseline=null;armed=false;clearArrows()}else{setTimeout(()=>{if(!baseline)capture()},250)}},true);const mo=new MutationObserver(()=>{if(!general()||!baseline||!armed)return;schedule('Manual party swap')});mo.observe(r,{childList:true,subtree:true});setTimeout(()=>{if(general()&&!baseline)capture()},1500)}
const style=()=>{if(document.getElementById('general-top-swap-style'))return;const s=document.createElement('style');s.id='general-top-swap-style';s.textContent='.general-top-swap-arrow{font-weight:800!important;margin-left:5px;white-space:nowrap;cursor:help!important;position:relative}.general-top-swap-arrow.general-swap-up{color:#65c878!important}.general-top-swap-arrow.general-swap-down{color:#ef6b6b!important}.general-top-swap-arrow:hover::after{content:attr(data-swap-title);position:absolute;left:0;bottom:calc(100% + 7px);z-index:99999;background:#17191d;color:#eee;border:1px solid rgba(255,255,255,.18);border-radius:6px;padding:7px 9px;font-size:11px;font-weight:400;white-space:nowrap;box-shadow:0 8px 24px rgba(0,0,0,.4)}';document.head.appendChild(s)};
function boot(){style();start()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,50),{once:true});else setTimeout(boot,50);
})();
