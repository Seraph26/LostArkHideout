/* Lost Ark Hideout — support hover data bridge v9 */
(()=>{
'use strict';
const SUPPORTS=new Set(['Bard','Artist','Paladin','Valkyrie']);
const clean=s=>String(s??'').replace(/\s+/g,' ').trim();
function classFor(member){const explicit=clean(member.querySelector('.class-icon')?.alt||member.dataset.class||member.querySelector('[data-class]')?.dataset.class||'');return SUPPORTS.has(explicit)?explicit:''}
function encounter(){try{const p=window.LostArkEncounterScoring?.profile?.();if(p?.name)return p}catch{};const s=document.getElementById('raidSpecificSelect');const t=clean(s?.selectedOptions?.[0]?.textContent);return t&&t!=='Select Raid'?t:'Selected encounter'}
async function render(){const members=[...document.querySelectorAll('#suggestedParties .party-member')];const supports=members.filter(m=>clean(m.querySelector('.party-role-label')?.textContent).toLowerCase()==='support').map(m=>({m,cls:classFor(m)})).filter(x=>x.cls);if(!supports.length)return;let stats=null;try{const mode=window.LostArkOptimizerMode;const enc=mode?.encounter;if(window.LostArkSupportStats?.fetch&&enc)stats=await window.LostArkSupportStats.fetch(enc);else if(window.LostArkSupportStats?.summary)stats={stats:{}}}catch{};for(const {m,cls} of supports){const summary=stats?.stats?.[cls]||window.LostArkSupportStats?.summary?.(cls);if(!summary)continue;const details=m.querySelectorAll('.character-hover-breakdown .chb-detail');if(!details.length)continue;const row=(label,v)=>`${label}: ${v!=null?((Number(v)*100).toFixed(2)+'%'):'Unavailable'}`;details[0].innerHTML=`<div>${clean(encounter())}</div><div>${row('Attack Power',summary.ap)} · ${row('Brand',summary.brand)} · ${row('H.A. Skill',summary.ha)} · ${row('Identity',summary.identity)}</div>`;for(let i=1;i<details.length;i++)details[i].remove()}}
function start(){let timer=0;const schedule=()=>{clearTimeout(timer);timer=setTimeout(()=>render(),150)};schedule();const root=document.getElementById('suggestedParties')||document.body;new MutationObserver(schedule).observe(root,{childList:true,subtree:true,characterData:true});document.getElementById('optimizeBtn')?.addEventListener('click',()=>{[300,800,1600,3000].forEach(ms=>setTimeout(render,ms))});document.getElementById('raidSpecificSelect')?.addEventListener('change',()=>setTimeout(render,300));}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
