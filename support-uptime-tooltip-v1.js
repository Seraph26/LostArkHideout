/* Lost Ark Hideout — Support Uptime party-summary authority only. */
(()=>{
'use strict';
const tooltipText='Party synergies available from the current party. Support uptime is an optimizer estimate based on support class type and party composition, including party positioning compatibility.';
const clean=s=>String(s??'').replace(/\s+/g,' ').trim();
const LABELS={damage:'Damage',defensereduction:'Defense Reduction',defenseReduction:'Defense Reduction',crit:'Critical Rate',critdamage:'Critical Damage',attackpower:'Attack Power',attackspeed:'Attack Speed',movespeed:'Move Speed',mana:'Mana',supportamplification:'Support Amplification',identity:'Identity'};
function canonical(v){const raw=clean(v), spaced=raw.replace(/([a-z])([A-Z])/g,'$1 $2').replace(/[._-]+/g,' ').replace(/\s+/g,' ').trim(), k=spaced.toLowerCase().replace(/\s+/g,'');return LABELS[k]||spaced.replace(/\b\w/g,c=>c.toUpperCase())}
function normalizeSummary(el){const text=clean(el.textContent);if(!/^Synergies\s*:/i.test(text))return;let body=text.replace(/^Synergies\s*:\s*/i,'');let uptime=(body.match(/Support uptime\s*([\d.]+)%?/i)||[])[1];const raid=body.match(/Observed median support uptime data\s*[·•]\s*identity median\s*([\d.]+)%?/i);if(!uptime&&raid)uptime=raid[1];body=body.replace(/\s*[·•]\s*Observed median support uptime data\s*[·•]\s*identity median\s*[\d.]+%?/ig,'').replace(/\s*[·•]\s*Observed median support uptime(?:\s+data)?/ig,'').replace(/\s*[·•]\s*identity median\s*[\d.]+%?/ig,'').replace(/\s*[·•]\s*Support uptime\s*[\d.]+%%?/ig,'');const parts=body.split(/\s*,\s*/).map(canonical).filter(Boolean);let desired=`Synergies: ${parts.join(', ')}`;if(uptime)desired+=` · Support uptime ${uptime}%`;el.textContent=desired}
function wrap(){document.querySelectorAll('#suggestedParties .party-synergies').forEach(el=>{normalizeSummary(el);if(el.querySelector('.support-uptime-label'))return;const walker=document.createTreeWalker(el,NodeFilter.SHOW_TEXT);const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);for(const node of nodes){const text=node.nodeValue||'',match=/Support uptime/i.exec(text);if(!match)continue;const before=text.slice(0,match.index),after=text.slice(match.index+match[0].length),frag=document.createDocumentFragment();if(before)frag.appendChild(document.createTextNode(before));const span=document.createElement('span');span.className='support-uptime-label';span.textContent='Support uptime';span.setAttribute('data-tooltip',tooltipText);span.setAttribute('aria-label',tooltipText);frag.appendChild(span);if(after)frag.appendChild(document.createTextNode(after));node.parentNode.replaceChild(frag,node);break}})}
function css(){let s=document.getElementById('support-uptime-tooltip-v1-style');if(!s){s=document.createElement('style');s.id='support-uptime-tooltip-v1-style';document.head.appendChild(s)}s.textContent='#suggestedParties .party-synergies .support-uptime-label{display:inline-block!important;text-decoration-line:underline!important;text-decoration-style:dotted!important;text-decoration-thickness:1px!important;text-underline-offset:3px!important;cursor:help!important;font:inherit!important;color:inherit!important}#lah-support-uptime-tooltip{position:fixed;display:none;width:360px;max-width:calc(100vw - 32px);padding:10px 12px;border-radius:6px;background:#1f1f1f;color:#fff;font-size:12px;line-height:1.45;z-index:2147483647;pointer-events:none}'}
function start(){css();wrap();const root=document.getElementById('suggestedParties')||document.body;let timer=0;const schedule=()=>{clearTimeout(timer);timer=setTimeout(wrap,30)};new MutationObserver(schedule).observe(root,{childList:true,subtree:true,characterData:true});document.addEventListener('mouseover',e=>{const el=e.target.closest?.('#suggestedParties .support-uptime-label');if(!el)return;let t=document.getElementById('lah-support-uptime-tooltip');if(!t){t=document.createElement('div');t.id='lah-support-uptime-tooltip';document.body.appendChild(t)}t.textContent=el.getAttribute('data-tooltip')||tooltipText;t.style.display='block';const r=el.getBoundingClientRect();t.style.left=Math.max(16,Math.min(r.left+r.width/2-t.offsetWidth/2,innerWidth-t.offsetWidth-16))+'px';t.style.top=Math.max(8,r.top-t.offsetHeight-10)+'px'},true);document.addEventListener('mouseout',e=>{if(e.target.closest?.('#suggestedParties .support-uptime-label')){const t=document.getElementById('lah-support-uptime-tooltip');if(t)t.style.display='none'}},true)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();