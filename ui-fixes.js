/* UI fixes intentionally left empty.
   Class icons are rendered by app-fixed.js from the Bible class-image source.
   Do not replace those URLs with a second icon system: doing so caused the
   roster/party icons to disappear for valid classes. */
(()=>{})();
