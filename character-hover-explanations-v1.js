/* Lost Ark Hideout — character hover explanations v11 */
(()=>{'use strict';
const LABELS={crit:'Critical Rate',critDamage:'Critical Damage',attackSpeed:'Attack Speed',attackPower:'Attack Power',supportAmplification:'Support Amplification',mana:'Mana',identity:'Identity',damage:'Damage',defenceReduction:'Defense Reduction',defenseReduction:'Defense Reduction',moveSpeed:'Move Speed',movementSpeed:'Move Speed',positional:'Positional Damage',brand:'Brand',ha:'H.A. Skill',ap:'Attack Power'};
const clean=s=>String(s??'').replace(/\s+/g,' ').trim();
const label=k=>LABELS[k]||k;
const numberFrom=s=>{const m=String(s??'').replace(/,/g,'').match(/[-+]?\d+(?:\.\d+)?/);return m?Number(m[0]):null};
function normalizeUptime(text){return String(text??'').replace(/\bobserved\s+median(?:\s+uptime)+/gi,'observed median');}
function humanizeEffects(text){let out=normalizeUptime(text);for(const[k,v]of Object.entries(LABELS))out=out.replace(new RegExp(`\\b${k}\\b`,'g'),v);return out.replace(/\bobserved\s+median\b/gi,'observed median uptime')}
function formatPartySynergySummary(el){const raw=clean(el.textContent);if(!/^Synergies\s*:/i.test(raw))return;let body=raw.replace(/^Synergies\s*:\s*/i,'');for(const k of Object.keys(LABELS).sort((a,b)=>b.length-a.length))body=body.replace(new RegExp(`\\b${k}\\b`,'g'),LABELS[k]);body=body.replace(/\bobserved\s+median\s+support\s+uptime\s+data\b/gi,'Observed median support uptime data').replace(/\bidentity\s+median\b/gi,'Identity median');el.innerHTML=`<strong>Synergies:</strong> ${body||'None'}`;el.dataset.chbSynergySummaryDone='1';el.title='Party synergies available from the current party. Support uptime figures are observed encounter data and are separate from the optimizer contribution model.';el.style.cursor='help'}
function formatEffectRow(row){const raw=clean(normalizeUptime(row.textContent));if(!raw||/^No direct party effects detected\.?$/i.test(raw))return;const m=raw.match(/^(.+?)\s+from\s+(.+?):\s*([+-]?[\d,]+(?:\.\d+)?)(?:\s*[·-]\s*observed median(?:\s+uptime)?\s*([\d.]+)%?)?$/i);if(!m){const h=humanizeEffects(raw);if(h!==clean(row.textContent))row.textContent=h;return}const effect=label(clean(m[1])),source=clean(m[2]),value=numberFrom(m[3]);if(!Number.isFinite(value))return;const head=row.closest('.character-hover-breakdown')?.querySelector('.chb-head span'),base=numberFrom(head?.textContent?.match(/CP\s+([\d,]+(?:\.\d+)?)/i)?.[1]),pct=Number.isFinite(base)&&base>0?value/base*100:0,uptime=Number.isFinite(numberFrom(m[4]))?numberFrom(m[4]):null;row.textContent=`${effect} from ${source}: ${value>=0?'+':''}${value.toLocaleString(undefined,{maximumFractionDigits:2})} estimated contribution · ${pct.toFixed(2)}% of base power${uptime!==null?` · observed median uptime ${uptime.toFixed(2)}%`:''}`;row.style.display='block';row.style.margin='2px 0'}
function formatLegacyGeneralCard(card){if(document.getElementById('generalOptimization')?.checked!==false&&window.LostArkOptimizerMode?.general!==false)return;if(card.dataset.chbGeneralDetailed==='1')return;const text=clean(card.textContent);if(!/\bContribution:\s*[\d,]+/i.test(text)||!/\bBase CP:\s*[\d,]+/i.test(text))return;const rows=[...card.querySelectorAll('div')].map(x=>clean(x.textContent)).filter(Boolean);const get=rx=>{const r=rows.find(x=>rx.test(x));return r?r.match(rx)?.[1]:null};const contribution=get(/Contribution:\s*([\d,]+(?:\.\d+)?)/i),base=get(/Base CP:\s*([\d,]+(?:\.\d+)?)/i),build=get(/Build Effect:\s*([+-]?[\d,]+(?:\.\d+)?)/i)||'0',synergy=get(/Party Synergy:\s*([+-]?[\d,]+(?:\.\d+)?)/i)||'0',support=get(/Support Impact:\s*([+-]?[\d,]+(?:\.\d+)?)/i)||'0';if(!contribution||!base)return;const name=clean(card.querySelector('strong')?.textContent||'Character'),b=numberFrom(base),total=numberFrom(contribution),bv=numberFrom(build),sv=numberFrom(synergy),supv=numberFrom(support),detail=v=>{const n=numberFrom(v)||0;return `${n>=0?'+':''}${n.toLocaleString(undefined,{maximumFractionDigits:2})} estimated contribution · ${b>0?(n/b*100).toFixed(2):'0.00'}% of base power`};card.className='character-hover-breakdown chb-general-detailed';card.innerHTML=`<div class="chb-head"><strong>${name}</strong><span>CP ${Math.round(b).toLocaleString()} · Contribution ${Math.round(total).toLocaleString()}</span></div><div class="chb-stats"><span class="chb-explained-metric"><span class="chb-metric-label">Base Power</span> ${b.toLocaleString()}</span><span class="chb-explained-metric"><span class="chb-metric-label">Build Effect</span> ${detail(bv)}</span><span class="chb-explained-metric"><span class="chb-metric-label">Party Synergy</span> ${detail(sv)}</span><span class="chb-explained-metric"><span class="chb-metric-label">Support Impact</span> ${detail(supv)}</span></div><div class="chb-detail"><strong>General Optimization model</strong> — contribution is the modeled potential for this character in the current party. General Optimization does not apply raid-specific encounter factors.</div>`;card.dataset.chbGeneralDetailed='1';card.title='General Optimization uses the general party scoring model. The values shown here are modeled contributions, not observed Bible DPS parses.'}
function enhance(){document.querySelectorAll('.character-hover-breakdown').forEach(card=>{formatLegacyGeneralCard(card);const stats=card.querySelector('.chb-stats'),head=card.querySelector('.chb-head'),contribution=head?.querySelector('span');if(contribution&&/^CP\s+[\d,]+\s*[·-]\s*Contribution\s+/i.test(clean(contribution.textContent))){contribution.title='Estimated overall contribution for this character in the current party. It is an optimizer model value, not an observed Bible DPS parse and not Combat Power.';contribution.style.cursor='help'}if(stats)[...stats.querySelectorAll('span')].forEach(span=>{const t=clean(span.textContent);if(/^Party Synergy\s+[+−-]/i.test(t)){span.title='Estimated increase to this character’s modeled potential from offensive synergies supplied by the other DPS characters in the party. This is a model contribution, not a direct in-game damage percentage.';span.classList.add('chb-explained-metric')}else if(/^Support(?: Impact)?\s+[+−-]/i.test(t)){span.title='Estimated increase to this character’s modeled potential from the party support. This is a model contribution, not a direct in-game damage percentage.';span.classList.add('chb-explained-metric')}});card.querySelectorAll('.chb-synergy').forEach(formatEffectRow);card.querySelectorAll('.chb-synergy').forEach(row=>{const txt=clean(row.textContent);row.title=/observed median uptime/i.test(txt)?'Estimated contribution is the optimizer model value assigned to this effect. Observed median uptime is the typical percentage of the relevant encounter for which the support effect is active, based on Bible encounter data.':'Estimated contribution from a party synergy. The numeric value is a model contribution value, not a percentage.'})});document.querySelectorAll('.party-synergies').forEach(formatPartySynergySummary);document.querySelectorAll('div,span').forEach(el=>{if(el.dataset.chbSynergySummaryDone==='1')return;if(/^Synergies\s*:/i.test(clean(el.textContent)))formatPartySynergySummary(el)})}
function css(){let s=document.getElementById('character-hover-explanations-style');if(!s){s=document.createElement('style');s.id='character-hover-explanations-style';document.head.appendChild(s)}s.textContent='.chb-explained-metric{cursor:help;border-bottom:1px dotted rgba(255,255,255,.45)}.chb-explained-metric:hover{border-bottom-color:currentColor}.chb-metric-label{font-weight:600}.chb-synergy{display:block!important;margin:2px 0}.chb-synergy[title]{cursor:help}.chb-detail{line-height:1.45;margin-top:6px;opacity:.85}.party-synergies{line-height:1.5}.party-synergies strong{font-weight:600}.chb-general-detailed{padding:6px 8px;border-radius:6px}'}
function start(){css();enhance();const root=document.getElementById('suggestedParties')||document.body;let timer;const schedule=()=>{clearTimeout(timer);timer=setTimeout(enhance,25)};new MutationObserver(schedule).observe(root,{childList:true,subtree:true,characterData:true});document.addEventListener('mouseover',e=>{if(e.target.closest?.('#suggestedParties .party-member'))schedule()},true);[0,50,150,300,600,1000,2000,4000,8000].forEach(ms=>setTimeout(enhance,ms));setInterval(enhance,1000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();})();

/* Canonical DPS hover bridge — General Optimization only. Raid-specific rendering is untouched. */
(()=>{'use strict';
const SUPPORTS=new Set(['Bard','Artist','Paladin','Valkyrie']);
const clean=s=>String(s??'').replace(/\s+/g,' ').trim(),num=v=>Number.isFinite(Number(v))?Number(v):0,fmt=v=>Math.round(num(v)).toLocaleString();
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const LABELS={damage:'Damage',mana:'Mana',crit:'Critical Rate',critDamage:'Critical Damage',attackSpeed:'Attack Speed',attackPower:'Attack Power',supportAmplification:'Support Amplification',positional:'Positional Damage'};
function active(){return document.getElementById('generalOptimization')?.checked!==false&&window.LostArkOptimizerMode?.general!==false}
function load(){try{return JSON.parse(localStorage.getItem('lostark-hideout-private-v3')||'null')||{characters:[]}}catch{return{characters:[]}}}
function roster(){return(load().characters||[]).filter(c=>c&&c.id)}
function profile(c){return c.profile||c.data||{}}
function build(c){try{return window.LostArkBuildProfilesV3?.get(c.url)||window.LostArkBuildProfilesV2?.get(c.url)||{}}catch{return{}}}
function text(c){const p=profile(c),b=build(c);return clean([p.rawText,p.text,p.characterText,p.arkGridText,p.arkPassiveText,p.engravingsText,p.gemsText,p.tripodsText,b.text,b.className,b.engravings?.join(' '),b.grid?.map(x=>x.name+' '+x.points).join(' '),b.arkPassive?.map(x=>x.name+' '+x.level).join(' ')].filter(Boolean).join(' ')).toLowerCase()}
function cls(c){const p=profile(c),b=build(c),v=clean(p.class||p.className||p.characterClass||b.className);if(v&&v.toLowerCase()!=='unknown')return({'Soul Eater':'Souleater','Arcana':'Arcanist','Glavier':'Glaivier'}[v]||v);const t=text(c),m=[['Summoner',/\bsummoner\b|master summoner|ancient spear/],['Souleater',/souleater|soul eater|full moon harvester|night.?s edge/],['Arcanist',/arcanist|arcana|empress grace|emperor.?s decree/],['Glaivier',/glaivier|glavier|pinnacle|control/],['Bard',/\bbard\b|desperate salvation|true courage/],['Artist',/\bartist\b|full bloom|recurrence/],['Paladin',/\bpaladin\b|blessed aura/],['Valkyrie',/\bvalkyrie\b/]];for(const[n,r]of m)if(r.test(t))return n;return'Unknown'}
function role(c){const p=profile(c);return p.role==='Support'||SUPPORTS.has(cls(c))?'Support':'DPS'}
function info(c){const p=profile(c);return{name:clean(p.name||c.name)||'Unknown',cls:cls(c),role:role(c),cp:num(p.cp??p.combatPower),url:c.url||p.url||''}}
function pos(c){const b=build(c),t=text(c);if(b.positional&&b.positional!=='Unknown')return b.positional;if(b.behavior?.positioning&&b.behavior.positioning!=='flexible')return b.behavior.positioning;if(/ambush master|back attack/.test(t))return'Back Attack';if(/master brawler|front attack/.test(t))return'Front Attack';if(/hit master/.test(t))return'Hit Master';if(/master summoner/.test(t)&&/summoner/.test(t))return'Hit Master';return'Unknown'}
function effects(c){try{const x=window.LostArkPartySynergyAuthorityV1?.provided?.(info(c).cls,build(c),text(c));if(Array.isArray(x)&&x.length)return x}catch{}return[]}
function weight(c,type){const t=text(c),p=pos(c);let w=1;if(type==='crit'&&/keen blunt|adrenaline|burst|full moon/.test(t))w+=.35;if(type==='critDamage'&&/keen blunt|burst/.test(t))w+=.30;if(type==='attackSpeed'&&/raid captain|swiftness/.test(t))w+=.35;if(type==='positional')w=p==='Back Attack'||p==='Front Attack'?1.35:p==='Hit Master'?.55:.85;if(type==='mana')w=/summoner|mana|boundless/.test(t)?1.55:.2;if(type==='damage'&&/summoner/.test(t))w+=.10;return w}
function supportEffects(c){switch(info(c).cls){case'Bard':return[{type:'supportAmplification',value:.10},{type:'mana',value:.12},{type:'attackSpeed',value:.035}];case'Artist':return[{type:'supportAmplification',value:.10},{type:'mana',value:.08},{type:'attackSpeed',value:.04}];case'Paladin':return[{type:'supportAmplification',value:.10},{type:'damage',value:.03}];case'Valkyrie':return[{type:'supportAmplification',value:.095},{type:'attackSpeed',value:.06}];default:return[{type:'supportAmplification',value:.09}]}}
function supportUptime(p,target){const s=p.find(x=>role(x)==='Support');if(!s)return 0;const sc=info(s).cls,ps=p.filter(x=>role(x)==='DPS').map(pos).filter(x=>x!=='Unknown'),mixed=new Set(ps).size>1;let u=(sc==='Paladin'||sc==='Valkyrie')?(mixed?.94:.99):(mixed?.76:.95);if((sc==='Bard'||sc==='Artist')&&pos(target)==='Hit Master'&&mixed)u-=.05;return Math.max(.60,Math.min(1,u))}
function contribution(p,target){if(role(target)!=='DPS')return null;const cp=info(target).cp,b=build(target),fit=(b.engravings&&b.engravings.length ? .004 : 0)+(b.grid&&b.grid.length ? .004 : 0)+(b.arkPassive&&b.arkPassive.length ? .004 : 0)+(/master summoner|communication overflow/.test(text(target)) ? .002 : 0);let synergy=0,support=0,details=[];for(const src of p.filter(c=>c!==target&&role(c)==='DPS'))for(const e of effects(src)){const v=cp*num(e.magnitude??e.value)*weight(target,e.type);synergy+=v;details.push({source:info(src).name,type:e.type,value:v})}const sup=p.find(c=>role(c)==='Support');let uptime=0;if(sup){uptime=supportUptime(p,target);for(const e of supportEffects(sup)){const v=cp*e.value*weight(target,e.type)*uptime;support+=v;details.push({source:info(sup).name,type:e.type,value:v,uptime})}}return{base:cp,build:cp*fit,synergy,support,total:cp+cp*fit+synergy+support,uptime,details}}
function parties(){const state=(()=>{try{return JSON.parse(localStorage.getItem('lostark-hideout-party-assignments-v2')||'null')}catch{return null}})()||{};const m=new Map(roster().map(c=>[c.id,c])),out=[];for(const ids of [state.party1||[],state.party2||[]])out.push(ids.filter(id=>m.has(id)).slice(0,4).map(id=>m.get(id)));return out}
function setCard(c,x){const card=c.querySelector('.character-hover-breakdown');if(!card||role(c)!=='DPS'||!x)return;const b=x.base||info(c).cp,sy=x.synergy||0,su=x.support||0;const rows=x.details.map(d=>`<div class="chb-synergy">${esc(LABELS[d.type]||d.type)} from ${esc(d.source)}: ${d.value>=0?'+':''}${fmt(d.value)} estimated contribution - ${(d.value/b*100).toFixed(2)}% of base power</div>`).join('');card.className='character-hover-breakdown chb-canonical-dps';card.innerHTML=`<div class="chb-head"><strong>${esc(info(c).name)}</strong><span>CP ${fmt(b)} · Contribution ${fmt(x.total)}</span></div><div class="chb-stats"><span>Party Synergy ${sy>=0?'+':''}${(sy/b*100).toFixed(2)}% Support Impact ${su>=0?'+':''}${(su/b*100).toFixed(2)}%</span></div>${rows?`<div class="chb-detail">${rows}</div>`:'<div class="chb-detail">No direct party effects detected.</div>'}`;card.dataset.generalCanonical='1';card.title='Canonical DPS hover formatting. General Optimization changes the data, not the hover format.'}
function render(){if(!active())return;for(const p of parties())if(p.length===4)for(const c of p)if(role(c)==='DPS')setCard(c,contribution(p,c))}
function css(){let s=document.getElementById('general-canonical-dps-style');if(!s){s=document.createElement('style');s.id='general-canonical-dps-style';document.head.appendChild(s)}s.textContent='.chb-canonical-dps{width:430px!important}.chb-canonical-dps .chb-head{display:flex;justify-content:space-between;gap:10px;margin-bottom:5px}.chb-canonical-dps .chb-stats{margin:3px 0 5px}.chb-canonical-dps .chb-detail{margin-top:5px}.chb-canonical-dps .chb-synergy{display:block;margin:2px 0}'}
function start(){css();render();const root=document.getElementById('suggestedParties')||document.body;let t;const schedule=()=>{clearTimeout(t);t=setTimeout(render,40)};new MutationObserver(schedule).observe(root,{childList:true,subtree:true,characterData:true});document.addEventListener('change',e=>{if(e.target?.id==='generalOptimization'||e.target?.id==='raidSpecificSelect')schedule()},true);[0,100,300,600,1200,2500].forEach(ms=>setTimeout(render,ms));setInterval(render,1000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();})();