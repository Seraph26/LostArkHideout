/* Lost Ark Hideout — General Optimization final hover renderer v2 */
(()=>{
'use strict';
const SUPPORTS=new Set(['Bard','Artist','Paladin','Valkyrie']);
const clean=s=>String(s??'').replace(/\s+/g,' ').trim(),num=v=>Number.isFinite(Number(v))?Number(v):0,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const label={damage:'Damage',mana:'Mana',crit:'Critical Rate',critDamage:'Critical Damage',attackSpeed:'Attack Speed',attackPower:'Attack Power',supportAmplification:'Support Amplification',positionalGeneral:'Positional Damage',positionalSpecific:'Positional Damage'};
function active(){return document.getElementById('generalOptimization')?.checked!==false&&window.LostArkOptimizerMode?.general!==false}
function load(){try{return JSON.parse(localStorage.getItem('lostark-hideout-private-v3')||'null')||{characters:[]}}catch{return{characters:[]}}}function roster(){return(load().characters||[]).filter(c=>c&&c.id)}function profile(c){return c.profile||c.data||{}}function build(c){try{return window.LostArkBuildProfilesV3?.get(c.url)||window.LostArkBuildProfilesV2?.get(c.url)||{}}catch{return{}}}
function text(c){const p=profile(c),b=build(c);return clean([p.rawText,p.text,p.characterText,p.arkGridText,p.arkPassiveText,p.engravingsText,p.gemsText,p.tripodsText,b.text,b.className,b.engravings?.join(' '),b.grid?.map(x=>x.name+' '+x.points).join(' '),b.arkPassive?.map(x=>x.name+' '+x.level).join(' '),b.tripods?.map(x=>x.skill+' '+x.name).join(' ')].filter(Boolean).join(' ')).toLowerCase()}
function cls(c){const p=profile(c),b=build(c),v=clean(p.class||p.className||p.characterClass||b.className);if(v&&v.toLowerCase()!=='unknown')return({'Soul Eater':'Souleater','Arcana':'Arcanist','Glavier':'Glaivier'}[v]||v);const t=text(c),m=[['Summoner',/\bsummoner\b|master summoner|ancient spear/],['Souleater',/souleater|soul eater|full moon harvester|night.?s edge/],['Arcanist',/arcanist|arcana|empress grace|emperor.?s decree/],['Glaivier',/glaivier|glavier|pinnacle|control/],['Bard',/\bbard\b|desperate salvation|true courage/],['Artist',/\bartist\b|full bloom|recurrence/],['Paladin',/\bpaladin\b|blessed aura/],['Slayer',/\bslayer\b|predator|punisher/],['Breaker',/\bbreaker\b|asura.?s path|brawl king/],['Deathblade',/\bdeathblade\b|surge|remaining energy/],['Gunlancer',/\bgunlancer\b|lone knight|combat readiness/],['Wardancer',/wardancer|battlemaster/],['Striker',/\bstriker\b/],['Aeromancer',/aeromancer/],['Sharpshooter',/sharpshooter|hawk/],['Destroyer',/destroyer/],['Artillerist',/artillerist/],['Reaper',/\breaper\b/],['Scrapper',/\bscrapper\b/],['Shadowhunter',/shadowhunter|demonic/],['Deadeye',/deadeye/],['Gunslinger',/gunslinger/],['Soulfist',/soulfist/],['Machinist',/machinist|scouter/]];for(const[n,r]of m)if(r.test(t))return n;return'Unknown'}
function role(c){const p=profile(c);return p.role==='Support'||SUPPORTS.has(cls(c))?'Support':'DPS'}function info(c){const p=profile(c);return{name:clean(p.name||c.name)||'Unknown',cls:cls(c),role:role(c),cp:num(p.cp??p.combatPower)}}
function pos(c){const b=build(c),t=text(c);if(b.positional&&b.positional!=='Unknown')return b.positional;if(b.behavior?.positioning&&b.behavior.positioning!=='flexible')return b.behavior.positioning;if(/ambush master|back attack/.test(t))return'Back Attack';if(/master brawler|front attack/.test(t))return'Front Attack';if(/hit master/.test(t))return'Hit Master';if(/master summoner/.test(t)&&/summoner/.test(t))return'Hit Master';return'Unknown'}
function effects(c){try{const x=window.LostArkPartySynergyAuthorityV1?.provided?.(info(c).cls,build(c),text(c));if(Array.isArray(x)&&x.length)return x}catch{}return[]}
function supportEffects(c){switch(info(c).cls){case'Bard':case'Artist':case'Paladin':return[{type:'supportAmplification',value:.10}];case'Valkyrie':return[{type:'supportAmplification',value:.095}];default:return[{type:'supportAmplification',value:.09}]}}
function weight(c,type){const t=text(c),p=pos(c);let w=1;if(type==='crit'&&/keen blunt|adrenaline|burst|full moon/.test(t))w+=.35;if(type==='critDamage'&&/keen blunt|burst/.test(t))w+=.30;if(type==='attackSpeed'&&/raid captain|swiftness/.test(t))w+=.35;if(type==='positionalGeneral'||type==='positionalSpecific')w=p==='Back Attack'||p==='Front Attack'?1.35:p==='Hit Master'?.55:.85;if(type==='mana')w=/summoner|mana|boundless/.test(t)?1.55:.2;if(type==='damage'&&/summoner/.test(t))w+=.10;return w}
function uptime(p,target){const s=p.find(x=>role(x)==='Support');if(!s)return 0;const sc=info(s).cls,ps=p.filter(x=>role(x)==='DPS').map(pos).filter(x=>x!=='Unknown'),mixed=new Set(ps).size>1;let u=(sc==='Paladin'||sc==='Valkyrie')?(mixed?.94:.99):(mixed?.76:.95);if((sc==='Bard'||sc==='Artist')&&pos(target)==='Hit Master'&&mixed)u-=.05;return Math.max(.60,Math.min(1,u))}
function calc(p,target){const cp=info(target).cp,details=[];let synergy=0,support=0;for(const src of p.filter(x=>x!==target&&role(x)==='DPS'))for(const e of effects(src)){const v=cp*num(e.magnitude??e.value)*weight(target,e.type);if(v>0){synergy+=v;details.push({source:info(src).name,type:e.type,value:v})}}const sup=p.find(x=>role(x)==='Support');let up=0;if(sup){up=uptime(p,target);for(const e of supportEffects(sup)){const v=cp*e.value*weight(target,e.type)*up;if(v>0){support+=v;details.push({source:info(sup).name,type:e.type,value:v,uptime:up})}}}return{synergy,support,details}}
function domParties(){const map=new Map(roster().map(c=>[c.id,c]));return[...document.querySelectorAll('#suggestedParties .party-dropzone')].map(z=>[...z.querySelectorAll(':scope > .party-member')].map(el=>map.get(el.dataset.characterId)).filter(Boolean))}
function renderDps(member,party){if(role(member)!=='DPS')return;const card=member.querySelector('.character-hover-breakdown');if(!card)return;const cp=info(member).cp;if(!cp)return;const r=calc(party,member),raw=clean(card.textContent),pm=raw.match(/Party Synergy\s*([+-]?[\d,]+(?:\.\d+)?)%/i),sm=raw.match(/Support Impact\s*([+-]?[\d,]+(?:\.\d+)?)%/i),cm=raw.match(/Contribution\s*:?\s*([\d,]+(?:\.\d+)?)/i),synergyPct=pm?num(pm[1]):r.synergy/cp*100,supportPct=sm?num(sm[1]):r.support/cp*100,contribution=cm?num(cm[1]):cp+r.synergy+r.support,sup=party.find(x=>role(x)==='Support'),supName=sup?info(sup).name:'';const rows=(rs,target)=>{const sum=rs.reduce((a,x)=>a+x.value,0);if(!sum)return'';return rs.map(x=>{const v=x.value/sum*target;return`<div class="gf-row"><div>${esc(label[x.type]||x.type)} from ${esc(x.source)}: ${v>=0?'+':''}${Math.round(v).toLocaleString()} estimated contribution</div><div>${(v/cp*100).toFixed(2)}% of base power${x.uptime!=null?` · observed median uptime ${(x.uptime*100).toFixed(2)}%`:''}</div></div>`}).join('')};const html=rows(r.details.filter(x=>x.source!==supName),synergyPct*cp/100)+rows(r.details.filter(x=>x.source===supName),supportPct*cp/100);card.innerHTML=`<div class="chb-head"><strong>${esc(info(member).name)}</strong><span>CP ${cp.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})} - Contribution ${Math.round(contribution).toLocaleString()}</span></div><div class="chb-stats"><span>Party Synergy +${synergyPct.toFixed(2)}%</span><span>Support Impact +${supportPct.toFixed(2)}%</span></div><div class="chb-detail">Support compatibility uses encounter data</div><div class="chb-detail">${html||'<div>No direct party effects detected.</div>'}</div>`;card.dataset.gfFinal='2'}
function renderSupport(member,party){if(role(member)!=='Support')return;const card=member.querySelector('.character-hover-breakdown');if(!card)return;const dps=party.filter(x=>role(x)==='DPS'),rows=[],total=dps.reduce((n,target)=>n+calc(party,target).support,0);for(const target of dps){const r=calc(party,target),v=r.support;if(v>0)rows.push(`<div class="gf-row"><div>Support Amplification to ${esc(info(target).name)}: +${Math.round(v).toLocaleString()} estimated contribution</div><div>${(v/info(target).cp*100).toFixed(2)}% of base power</div></div>`)}card.innerHTML=`<div class="chb-head"><strong>${esc(info(member).name)}</strong><span>CP ${info(member).cp.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</span></div><div class="chb-detail">Support contribution to this party: +${Math.round(total).toLocaleString()}</div><div class="chb-detail">${rows.join('')||'No direct support effects detected.'}</div>`;card.dataset.gfFinal='2'}
function render(){if(!active())return;for(const p of domParties())for(const m of p)role(m)==='Support'?renderSupport(m,p):renderDps(m,p)}
function css(){let s=document.getElementById('gf-final-style');if(!s){s=document.createElement('style');s.id='gf-final-style';document.head.appendChild(s)}s.textContent='.gf-row{margin:5px 0}.gf-row>div+div{opacity:.82}.chb-detail{line-height:1.45;margin-top:6px}.chb-head{display:flex;justify-content:space-between;gap:10px;margin-bottom:5px}.chb-stats{display:flex;gap:14px;margin:4px 0}.character-hover-breakdown{width:430px!important}'}
function start(){css();const run=()=>{if(active())render()};[0,100,250,500,1000,2000,4000].forEach(ms=>setTimeout(run,ms));setInterval(run,1000);const root=document.getElementById('suggestedParties')||document.body;new MutationObserver(()=>setTimeout(run,0)).observe(root,{childList:true,subtree:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
