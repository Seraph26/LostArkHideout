/* Lost Ark Hideout — Upgrade Potential v3
 * Informational only. Never contributes to party scoring or optimization.
 * Uses only explicit structured data exposed by the Bible build parser.
 * A gain is shown only when a defensible calculation exists; otherwise the opportunity is listed without a fabricated percentage.
 */
(()=>{
'use strict';
const STORE='lostark-hideout-private-v3';
const clean=s=>String(s??'').replace(/\s+/g,' ').trim();
function load(k,f){try{return JSON.parse(localStorage.getItem(k)||'null')||f}catch{return f}}
function roster(){return(load(STORE,{characters:[]}).characters||[]).filter(c=>c&&c.url)}
function build(c){try{return window.LostArkBuildProfilesAuthorityV1?.get(c.url)||window.LostArkBuildProfilesV3?.get(c.url)||window.LostArkBuildProfilesV2?.get(c.url)||{}}catch{return{}}}
function profile(c){return c.profile||c.data||{}}
function name(c){return clean(profile(c).name||profile(c).characterName||c.name)||'Unknown'}
function gridOpportunities(c){const b=build(c),out=[];if(!Array.isArray(b.grid)||!b.grid.length)return out;const order=b.grid.filter(x=>String(x.type||'').toLowerCase()==='order'),chaos=b.grid.filter(x=>String(x.type||'').toLowerCase()==='chaos');const orderMax=order.length?Math.max(...order.map(x=>Number(x.points)||0)):0;const chaosMax=chaos.length?Math.max(...chaos.map(x=>Number(x.points)||0)):0;if(order.length&&orderMax<20)out.push({category:'Ark Grid',label:`Order core: ${orderMax}/20 points toward the next threshold`,gain:null,confidence:'High',reason:'Current Order-core points are explicitly present in the Bible build data.'});if(chaos.length&&chaosMax<17)out.push({category:'Ark Grid',label:`Chaos core: ${chaosMax}/17 points toward the next threshold`,gain:null,confidence:'High',reason:'Current Chaos-core points are explicitly present in the Bible build data.'});return out}
function arkPassiveOpportunities(c){const b=build(c);if(!Array.isArray(b.arkPassive)||!b.arkPassive.length)return[];return b.arkPassive.filter(x=>Number(x.level)>0).map(x=>({category:'Ark Passive',label:`${clean(x.name)} Lv. ${x.level}: higher level may be available`,gain:null,confidence:'Low',reason:'The current node level is known, but the parser does not yet know the attainable next level and isolated damage contribution.'}))}
function parseGemLine(line){const s=clean(line);let m=s.match(/^(.*?)\s+(?:Damage|Cooldown|DMG|CD)\s*Gem\s*(?:Lv\.?\s*)?(\d{1,2})$/i);if(m)return{skill:clean(m[1]),type:/cooldown|cd/i.test(s)?'Cooldown':'Damage',level:+m[2],source:s};m=s.match(/^(?:Lv\.?\s*)?(\d{1,2})\s+(.*?)\s+(?:Damage|Cooldown|DMG|CD)\s*Gem$/i);if(m)return{skill:clean(m[2]),type:/cooldown|cd/i.test(s)?'Cooldown':'Damage',level:+m[1],source:s};m=s.match(/^(.*?)\s+(?:Damage|Cooldown|DMG|CD)\s+(\d{1,2})$/i);if(m)return{skill:clean(m[1]),type:/cooldown|cd/i.test(s)?'Cooldown':'Damage',level:+m[2],source:s};return null}
function gemOpportunities(c){const b=build(c),rows=Array.isArray(b.sections?.Gems)?b.sections.Gems:[];if(!rows.length)return[];const gems=[];for(const row of rows){const g=parseGemLine(row);if(g&&g.level>=1&&g.level<10)gems.push(g)}return gems.map(g=>({category:'Gem',label:`${g.skill} ${g.type} Gem Lv. ${g.level} → Lv. 10`,gain:null,confidence:'High',reason:`The Bible build explicitly exposes the current gem level (${g.level}). The isolated DPS/CD gain is not estimated until the exact skill weighting and gem mechanics are modeled.`}))}
function sectionOpportunity(c,key,category){const b=build(c),rows=Array.isArray(b.sections?.[key])?b.sections[key]:[];if(!rows.length)return[];return[]}
function tripodOpportunities(){return[]}
function accessoryOpportunities(c){return sectionOpportunity(c,'Accessories','Accessory')}
function braceletOpportunities(c){return sectionOpportunity(c,'Bracelet','Bracelet')}
function stoneOpportunities(c){return sectionOpportunity(c,'Ability Stone','Ability Stone')}
function opportunities(c){return[...gridOpportunities(c),...arkPassiveOpportunities(c),...gemOpportunities(c),...tripodOpportunities(c),...accessoryOpportunities(c),...braceletOpportunities(c),...stoneOpportunities(c)]}
function combinedGain(items){const quantified=items.filter(x=>Number.isFinite(x.gain));if(!quantified.length)return null;return(quantified.reduce((p,x)=>p*(1+x.gain),1)-1)*100}
function character(c){const items=opportunities(c);return{character:name(c),items,quantifiedGain:combinedGain(items)}}
function summary(){const chars=roster().map(character).filter(x=>x.items.length);return{chars,items:chars.flatMap(x=>x.items.map(i=>({...i,character:x.character}))),total:combinedGain(chars.flatMap(x=>x.items))}}
function row(x){return`<div class="upgrade-potential-row"><span><strong>${clean(x.category)}</strong> — ${clean(x.label)}<small>${clean(x.reason)}</small></span><strong>${Number.isFinite(x.gain)?'+'+(x.gain*100).toFixed(2)+'%':'Gain not quantified'}</strong></div>`}
function htmlFor(c){const items=opportunities(c);if(!items.length)return'<strong>Upgrade Potential</strong><div class="upgrade-potential-note">No specific upgrade opportunity is currently detectable from the available Bible data.</div>';return`<strong>Upgrade Potential</strong><div class="upgrade-potential-note">Informational only. These opportunities do not affect party optimization. Percentages are withheld unless the current data supports a defensible calculation.</div>${items.map(row).join('')}<div class="upgrade-potential-total"><span>Character quantified gain</span><strong>Not quantified</strong></div>`}
function tooltipHtml(){const s=summary();if(!s.items.length)return'<strong>Upgrade Potential</strong><div class="upgrade-potential-note">No specific upgrade opportunity is currently detectable from the available Bible data.</div>';return`<strong>Upgrade Potential</strong><div class="upgrade-potential-note">Informational only. Party optimization uses current power and compatibility, not future upgrades.</div>${s.items.map(row).join('')}<div class="upgrade-potential-total"><span>Combined quantified gain</span><strong>${s.total==null?'Not quantified':'+'+s.total.toFixed(2)+'%'}</strong></div>`}
function forCharacter(c){return{items:opportunities(c),html:htmlFor(c)}}
function styles(){if(document.getElementById('upgrade-potential-v3-style'))return;const s=document.createElement('style');s.id='upgrade-potential-v3-style';s.textContent=`.upgrade-potential-row{display:flex;justify-content:space-between;gap:14px;padding:5px 0;border-bottom:1px solid rgba(255,255,255,.07)}.upgrade-potential-row>span{min-width:0}.upgrade-potential-row strong{white-space:nowrap}.upgrade-potential-row small{display:block;margin-top:2px;color:#999;font-size:10px;line-height:1.35}.upgrade-potential-note{margin:5px 0 8px;color:#aaa;font-size:11px;line-height:1.4}.upgrade-potential-total{display:flex;justify-content:space-between;gap:12px;margin-top:9px;padding-top:8px;border-top:1px solid rgba(255,255,255,.18)}`;document.head.appendChild(s)}
function apply(){styles();for(const n of document.querySelectorAll('.upgrade-potential-trigger')){const label=n.querySelector('.optimizer-definition-label'),tip=n.querySelector('.optimizer-definition');if(label&&clean(label.textContent)==='Upgrade Potential'&&tip)tip.innerHTML=tooltipHtml()}}
function start(){apply();const root=document.getElementById('suggestedParties')||document.body;let queued=false;new MutationObserver(()=>{if(queued)return;queued=true;setTimeout(()=>{queued=false;apply()},50)}).observe(root,{childList:true,subtree:true});window.addEventListener('lostark-build-profiles-v3-ready',()=>setTimeout(apply,100))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
window.LostArkUpgradePotentialV1={summary,opportunities,forCharacter,tooltipHtml};
})();
